import React, { useState } from 'react';

const CitySearch = ( { getAirQuality } ) => {
    return (
        <form>
            <input></input>
            <button type='submit'>Search</button>
        </form>
    )
}